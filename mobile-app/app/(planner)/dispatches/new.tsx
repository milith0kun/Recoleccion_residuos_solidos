import React from 'react';
import { PlaceholderScreen } from '../../../src/components/PlaceholderScreen';

export default function PlannerNewDispatch() {
  return (
    <PlaceholderScreen
      title="Asignar salida"
      section="Operador"
      icon="send"
      message="Acá vas a poder elegir una ruta, asignar un conductor y un vehículo y fijar la fecha y hora de la salida."
    />
  );
}
