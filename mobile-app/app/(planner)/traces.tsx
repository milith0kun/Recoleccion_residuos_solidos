import React from 'react';
import { PlaceholderScreen } from '../../src/components/PlaceholderScreen';

export default function PlannerTraces() {
  return (
    <PlaceholderScreen
      title="Trazas históricas"
      section="Operador"
      icon="git-branch"
      message="Vas a poder ver, elegir y editar las trazas que el ciudadano ve en el mapa público."
    />
  );
}
